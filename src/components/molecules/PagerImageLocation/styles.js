import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    fontSize: 20,
    color: '#9485AC',
    marginTop: 10,
    marginBottom: -20,
    marginLeft: 15,
  },
  container: {flex: 1},
});

export default styles;
