import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  title: {
    fontSize: 17,
    fontWeight: 'bold',
    paddingBottom: 10,
  },
});

export default styles;
