import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 23,
    fontWeight: 'bold',
    marginVertical: 20,
    alignSelf: 'center',
  },
});

export default styles;
