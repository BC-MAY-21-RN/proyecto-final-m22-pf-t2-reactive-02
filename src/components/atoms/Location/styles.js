import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    margin: 10,
  },
  city: {
    fontSize: 17,
  },
  info: {
    marginLeft: 10,
  },
});

export default styles;
