import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    margin: 10,
  },
  name: {
    fontSize: 15,
    fontWeight: 'bold',
  },
  info: {
    marginLeft: 20,
    maxWidth: 200,
  },
});

export default styles;
