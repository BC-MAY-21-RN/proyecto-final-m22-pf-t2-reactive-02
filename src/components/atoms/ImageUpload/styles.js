import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 30,
  },
  button: {
    marginHorizontal: 20,
  },
});

export default styles;
